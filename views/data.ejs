<!DOCTYPE html>
<html>
    <div class="text-center center-div" id="login">
        <div class="container w-25 border py-5">
            <div class="title pb-5">
                <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
                
<head>
	
</head>

<body>
  
    <table class="table">
        <thead>
          <tr>
           
            <th>Name</th>
           
            <th>Password </th>
          </tr>
        </thead>
        <tbody>
           
            <%const result = [] %>
            <% for (var i = 0; i < user.length; i++) { %>
            
                <tr>
                    <td><%= user[i].username %></td>
                    <td><%= user[i].password %> </td>
                </tr>
            <%  } %>
            <div>
                <p>Add New User</p>
            </div>
            <div class="form-group ">
                <form action="/data" method="POST" class="pt-3" id="newUser">
                    <input type="text" name="newName" placeholder="Username" required="" class="form-control"><br />
                    <input type="text" name="newPassword" placeholder="Password" required="" class="form-control"><br />
                    <input type="submit" value="newUser" class="btn btn-success">
                </form>
            </div>

          </tbody>
        </table>


</body>
            </div>
        </div>
        </div>
</html>